import React from 'react';

const Task = () => {
    return (
        <div className="task">
          <span>Task</span>
          <button type="button">Delete</button>
        </div>
    );
};

export default Task;